#CREATE DATABASE node;
#use node;

#CREATE TABLE notes (id INT(8) NOT NULL AUTO_INCREMENT, note VARCHAR(50) NOT NULL, PRIMARY KEY (id));
#INSERT INTO notes (note) VALUES (("This is a test!"), ("This is another test..."), ("And, yet again, another..."));
SELECT * FROM messages;